<Window x:Class="StoreOwnerApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StoreOwnerApplication"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        ResizeMode="NoResize"
        WindowStyle="None"
        MouseDown="Window_MouseDown"
        BorderBrush="Transparent"
        >

    <Window.Resources>
        <Style x:Key="Links" TargetType="Hyperlink">
            <Setter Property="FontSize" Value="20"></Setter>
            <Setter Property="TextDecorations" Value="None"></Setter>
            <Setter Property="Foreground" Value="White"></Setter>
        </Style>
    </Window.Resources>
    <Border BorderBrush="Transparent" BorderThickness="2">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Height="60" Width="800" Background="Black">
                <TextBlock Margin="1,-8,0,0">
                    <Hyperlink Style="{StaticResource Links}" Foreground="Red" x:Name="ExitBtn" Click="Exitbtn" FontSize="20" TextDecorations="None" >x</Hyperlink>
                </TextBlock>
                <Label Margin="2, 10 ,20,15" Content="GymMachineStore" HorizontalAlignment="Left" Foreground="White" FontSize="20"></Label>
                <TextBlock Margin="350, 10 ,20,15" FontSize="20" Height="31">
                    <Hyperlink Style="{StaticResource Links}" Click="EmailLogin_Click">
                        Login
                    </Hyperlink>
                </TextBlock>
            <TextBlock Margin="0,10,20,15" Height="31" >
                <Hyperlink Style="{StaticResource Links}" x:Name="StartPage" Click="StartPage_Click" FontSize="20">
                    HomePage
                </Hyperlink>
            </TextBlock>
                <Label x:Name="label" Content="Label"/>
            </StackPanel>
        <Frame x:Name="ContentFrame" Height="400" Width="800" NavigationUIVisibility="Hidden"/>
    </StackPanel>
    </Border>
</Window>
