<UserControl x:Class="StoreOwnerApplication.Frames.MacinesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StoreOwnerApplication.Frames"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="630">

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Gym Machines" FontWeight="Black" FontSize="36" 
                   HorizontalAlignment="Center" Margin="0,20,0,10" Foreground="#333"/>

        <Grid Grid.Row="1" Margin="30,10,30,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchTextBox" Padding="5" Height="30" 
                     Text="Search..." Foreground="Gray" VerticalContentAlignment="Center"/>

            <Button Grid.Column="1" Content="+ Add Machine" Background="#007ACC" Foreground="White" 
                    FontWeight="Bold" Padding="10,5" Margin="10,0,0,0"
                    Click="AddMachine_Click"/>
            
        </Grid>

        <ListView x:Name="MachinesListView" Grid.Row="2" Margin="30,10" 
                  BorderThickness="0" Background="Transparent"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" CornerRadius="5" Margin="0,5" Padding="10" BorderBrush="#DDD" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="450"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding MachineName}" FontWeight="Bold" FontSize="16" />
                                <TextBlock Text="{Binding MachinePrice}" Foreground="Gray" FontSize="12"/>
                            </StackPanel>

                            <Button Grid.Column="1" Content="Delete" Background="#FF4444" Foreground="White" 
                                    Padding="10,5" BorderThickness="0"
                                    Click="DeleteMachine_Click" Tag="{Binding}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>