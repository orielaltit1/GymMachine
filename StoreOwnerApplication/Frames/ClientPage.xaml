<UserControl x:Class="StoreOwnerApplication.Frames.ClientPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StoreOwnerApplication.Frames"
             mc:Ignorable="d" 
             Height="450" Width="800">
    <Grid Background="#F5F5F5" Margin="60,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Clients List" FontWeight="Black" FontSize="36" 
               HorizontalAlignment="Center" Margin="0,20,0,10" Foreground="#333"/>

        <Grid Grid.Row="1" Margin="30,10,30,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchTextBox" Padding="5" Height="30" 
                 Text="Search..." Foreground="Gray" VerticalContentAlignment="Center"/>

        </Grid>

        <ListView x:Name="ClientsListView" Grid.Row="2" Margin="30,10" 
              BorderThickness="0" Background="Transparent"
              ScrollViewer.HorizontalScrollBarVisibility="Disabled" d:ItemsSource="{d:SampleData ItemCount=3}" >

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" CornerRadius="5" Margin="0,2" Padding="5" BorderBrush="#DDD" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="650"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding FullName}" FontWeight="Bold" FontSize="16" Margin="9, 2,0 ,20" />
                            </StackPanel>
                        
                            <TextBlock Text="{Binding ClientGender}" Foreground="Gray" FontSize="12" Margin="9,34,0,6" d:LayoutOverrides="Width"/>
                            <TextBlock Text="{Binding ClientEmail}" Foreground="Gray" FontSize="12" Margin="90,34,286,-3" Width="874"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>
        <Frame x:Name="MainFrame"
           Grid.Row="0"
           Grid.RowSpan="3"
           Background="White"
           NavigationUIVisibility="Hidden"
           Visibility="Collapsed"
           Panel.ZIndex="10"/>

    </Grid>
</UserControl>
